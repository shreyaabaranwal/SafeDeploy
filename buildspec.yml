version: 0.2

env:
  variables:
    AWS_DEFAULT_REGION: us-east-1
    ACCOUNT_ID: 606639293354
    BACKEND_REPO: safedeploy-backend
    FRONTEND_REPO: safedeploy

phases:
  pre_build:
    commands:
      - echo Logging into ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com

  build:
    commands:
      - echo Building Backend...
      - docker build -t backend -f deployments/docker/backend.Dockerfile .

      - echo Building Frontend...
      - docker build -t frontend -f deployments/docker/frontend.Dockerfile .

  post_build:
    commands:
      - echo Tagging...

      - BACKEND_IMAGE=$ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$BACKEND_REPO:latest
      - FRONTEND_IMAGE=$ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$FRONTEND_REPO:latest

      - docker tag backend:latest $BACKEND_IMAGE
      - docker tag frontend:latest $FRONTEND_IMAGE

      - docker push $BACKEND_IMAGE
      - docker push $FRONTEND_IMAGE

      - echo Creating JSON...

      - |
        cat <<EOF > imagedefinitions.json
        [
          {
            "name": "backend",
            "imageUri": "$BACKEND_IMAGE"
          },
          {
            "name": "frontend",
            "imageUri": "$FRONTEND_IMAGE"
          }
        ]
        EOF

      - echo "=== DEBUG JSON ==="
      - cat imagedefinitions.json

artifacts:
  files:
    - imagedefinitions.json
